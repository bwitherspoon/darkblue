name: darkblue
description: A custom uBlue image.

base-image: ghcr.io/ublue-os/silverblue-main
image-version: 39

modules:
  - type: files
    files:
      - usr: /usr

  - type: rpm-ostree
    repos:
    install:
    remove:
      - firefox
      - firefox-langpacks
      - gnome-tour

  - type: bling
    install:
      - nix-installer

  - type: yafti
    custom-flatpaks:

  - type: script
    scripts:
      - signing.sh
